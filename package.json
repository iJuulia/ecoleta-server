{
  "name": "server",
  "module": "es5",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "generate": "bunx drizzle-kit generate",
    "migrate": "bunx drizzle-kit migrate",
    "seed": "bun run src/db/seed.ts",
    "studio": "bun drizzle-kit studio",
    "reset": "rm -rf drizzle sqlite.db"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "drizzle-orm": "^0.33.0",
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/bun": "latest",
    "drizzle-kit": "^0.24.2"
  }
}
